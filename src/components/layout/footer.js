const renderFooter = () => {
  const footer = `
      <footer class="footer mt-auto py-3">
      <div class="container">
        <span class="text-muted">About us | Contact</span>
      </div>
    </footer>`;
  return footer;
};

export { renderFooter };
